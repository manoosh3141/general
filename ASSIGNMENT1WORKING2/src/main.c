#include "trig.h"

int main(void)
{
	// Print welcome message and initial prompt
	printf("TRIG: the trigonometric calculator\n\n");

	// Get the input which is in turn sent to trig.h to be processed
	getInput();
}